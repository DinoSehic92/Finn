<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Width="700" Height="420"
        MinWidth="460" MinHeight="340"
        xmlns:ic="using:FluentIcons.Avalonia"
        xmlns:model="clr-namespace:Finn.Model"
        x:Class="Finn.Dialogs.xVersionImportDia"
        Title=""
        xmlns:vm="clr-namespace:Finn.ViewModels"
        CanResize="True"
        WindowStartupLocation="CenterScreen"
        ExtendClientAreaToDecorationsHint="True"
        x:DataType="vm:MainViewModel">

	<Window.Styles>
		<Style Selector="Button.accent">
			<Setter Property="CornerRadius" Value="{Binding UI.CornerRadius}"/>
			<Setter Property="Background" Value="{DynamicResource SystemAccentColor}"/>
			<Setter Property="BorderThickness" Value="{Binding UI.BorderThickness}"/>
			<Setter Property="BorderBrush" Value="{DynamicResource DataGridGridLinesBrush}"/>
			<Setter Property="FontWeight" Value="SemiBold"/>
			<Setter Property="FontSize" Value="15"/>
		</Style>
		<Style Selector="Button.normal">
			<Setter Property="Background" Value="{DynamicResource TextControlBackground}"/>
			<Setter Property="Foreground" Value="{DynamicResource TextControlForeground}"/>
			<Setter Property="BorderThickness" Value="{Binding UI.BorderThickness}"/>
			<Setter Property="BorderBrush" Value="{DynamicResource DataGridGridLinesBrush}"/>
			<Setter Property="CornerRadius" Value="{Binding UI.CornerRadius}"/>
			<Setter Property="FontWeight" Value="SemiBold"/>
			<Setter Property="FontSize" Value="15"/>
		</Style>
	</Window.Styles>

	<Grid RowDefinitions="30,Auto,Auto,*,Auto" Margin="0">
		<Panel Grid.Row="0" IsHitTestVisible="False"/>

		<!-- Header -->
		<StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center" Spacing="6" Margin="0,10,0,12">
			<Border Width="48" Height="48" CornerRadius="24" Background="{DynamicResource SystemAccentColor}" HorizontalAlignment="Center">
				<ContentControl>
					<ic:SymbolIcon Symbol="History" FontSize="32" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
				</ContentControl>
			</Border>
			<TextBlock Text="Version Import" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,4,0,0"/>
			<TextBlock Text="These files match existing entries and will be added as versions."
					   FontSize="12" Opacity="0.7" HorizontalAlignment="Center" TextWrapping="Wrap" MaxWidth="400"/>
		</StackPanel>

		<!-- Bulk label setter -->
		<StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8" Margin="12,0,12,6">
			<TextBlock Text="Set all labels:" VerticalAlignment="Center" FontSize="12" Opacity="0.7"/>
			<ComboBox Name="BulkLabelCombo"
					  ItemsSource="{x:Static model:FileVersionData.VersionLabels}"
					  PlaceholderText="Choose…"
					  SelectionChanged="OnBulkLabelChanged"/>
		</StackPanel>

		<!-- DataGrid -->
		<DataGrid Name="VersionGrid" Grid.Row="3"
				  AutoGenerateColumns="False" IsReadOnly="False"
				  GridLinesVisibility="None" SelectionMode="Single"
				  CanUserReorderColumns="False" CanUserSortColumns="False"
				  Margin="5" VerticalAlignment="Stretch" RowHeight="22">
			<DataGrid.Styles>
				<Style Selector="DataGridCell:current /template/ Grid#FocusVisual">
					<Setter Property="IsVisible" Value="false"/>
				</Style>
			</DataGrid.Styles>
			<DataGrid.Columns>
				<DataGridTextColumn Header="File" Binding="{ReflectionBinding FileName}" Width="Auto" IsReadOnly="True"/>
				<DataGridTextColumn Header="Existing Versions" Binding="{ReflectionBinding ExistingVersions}" Width="Auto" IsReadOnly="True"/>
				<DataGridTemplateColumn Header="Label" Width="*" MinWidth="180">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate x:DataType="model:VersionImportEntry">
							<ComboBox ItemsSource="{x:Static model:FileVersionData.VersionLabels}"
									  SelectedItem="{Binding SelectedLabel, Mode=TwoWay}"
									  VerticalAlignment="Stretch"
									  HorizontalAlignment="Stretch"
									  MinWidth="100"
									  Margin="4,0"/>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
			</DataGrid.Columns>
		</DataGrid>

		<!-- Buttons -->
		<StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Spacing="8" Margin="0,12,0,16">
			<Button Name="AcceptButton" HotKey="Enter" Width="90" Classes="accent" Content="Accept" Click="OnAccept"/>
			<Button Name="CancelButton" HotKey="Escape" Width="90" Classes="normal" Content="Cancel" Click="OnCancel"/>
		</StackPanel>
	</Grid>

</Window>
